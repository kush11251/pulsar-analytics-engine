# Pulsar Analytics Engine
A scalable, real-time analytics engine for processing large datasets.
## Getting Started
1. Run `go mod init pulsar-analytics-engine` to initialize the Go module.
2. Run `go get` to install dependencies.
3. Run `go build` to compile the code.
4. Run `go run src/main.go` to start the server.
